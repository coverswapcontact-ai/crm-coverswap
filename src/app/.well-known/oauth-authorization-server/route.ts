/** RFC 8414 : métadonnées du serveur d'autorisation, à la racine du domaine (même document que /api/oauth/serveur). */
export { GET, OPTIONS } from "@/app/api/oauth/serveur/route";
export const dynamic = "force-dynamic";
