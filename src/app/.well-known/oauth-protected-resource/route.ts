/** RFC 9728 : métadonnées de la ressource protégée, à la racine du domaine (même document que /api/oauth/ressource). */
export { GET, OPTIONS } from "@/app/api/oauth/ressource/route";
export const dynamic = "force-dynamic";
