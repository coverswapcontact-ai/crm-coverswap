import ProspectionDashboard from "./ProspectionDashboard";

// Étape 1 : dashboard sur données mockées (voir mockData.ts).
// Le branchement DB/pipeline arrive à l'étape 2.
export default function ProspectionPage() {
  return <ProspectionDashboard />;
}
